/**
 * @deprecated Use `LocalStorageService` from `core/infrastructure/local-storage.service` instead.
 * This re-export exists for backward compatibility during migration.
 */
export { LocalStorageService } from '../infrastructure/local-storage.service';
